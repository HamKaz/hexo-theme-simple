<!-- navigation -->
<% site.pages.forEach(function(item, index) { %>
  <% if (item.category === theme.recommendation) { %>
    <div class="col-12 col-md-4 mx-0 my-0 px-0">
      <nav class="card mx-2 my-2">
        <section class="row mx-3 my-3">
          <%- partial('parts/cover_image', { item: item }) %>
        </section>
        <section class="row mx-auto mx-3 my-0">
          <%- partial('parts/title', { item: item }) %>
        </section>
        <section class="row mx-auto mx-3 my-3">
          <%- partial('parts/button_readmore', { item: item }) %>
        </section>
      </nav>
    </div>
  <% }; %>
<% }); %>
